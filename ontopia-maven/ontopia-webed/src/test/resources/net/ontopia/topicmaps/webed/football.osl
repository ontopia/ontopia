<!-- this schema accepts most things -->
<tm-schema match="loose">
  <topic match="loose">
    <instanceOf>
      <internalTopicRef href="#player"/>
    </instanceOf>

    <!-- must have exactly one unscoped name -->
    <baseName min="1" max="1">
      <scope/>
    </baseName>
  </topic>
</tm-schema>