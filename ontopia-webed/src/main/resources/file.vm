## Template used by net.ontopia.topicmaps.webed.taglibs.form.FileTag
## -----------------------------------------------------------------------
#if ($readonly)
  ${value}
#else
  <input type="file" name="${name}" #condAttribute("id" ${id}) #condAttribute("class" ${class}) #condAttribute("value" ${value})>
#end

