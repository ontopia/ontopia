<?xml version="1.0" encoding="ISO-8859-1"?>
<!--  This is a bare bones config file with just one action group
       which does not have a defaultForward. This should cause
       an error to occur when loading-->

<actionConfig>

  <classMap>
    <class shortcut="DefaultAction"
           fullname="net.ontopia.topicmaps.webed.impl.actions.DefaultAction"/>
  </classMap>
  
  <actionGroup name="default">
    <action name="default" class="DefaultAction"/>
  </actionGroup>

</actionConfig>
