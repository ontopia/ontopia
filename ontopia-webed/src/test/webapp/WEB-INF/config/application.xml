<configuration type="application">
  <properties>
    <property name="allowLoadOnRequest" value="true"/>
    
    <!-- NB! Some test cases with multiple forms may take some time to run, 
      so this property should not be set too low.
      If it's set too low, some tests might fail due to expiry. -->
    <property name="userBundleExpiryTime" value="2000"/>
  </properties>
</configuration>