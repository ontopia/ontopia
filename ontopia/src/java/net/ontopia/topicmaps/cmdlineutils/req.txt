Requirements, kind of


     - stats
       * report number of topics, associations, occurrences
       * above broken down by type, ordered alphabetically within
         each group (Not alphabetivally yer)
       * for each association type, report all existing combinations of 
         association roles (number of occurrences of each)
     - sanity
       - duplicate assocs (Steve was bitten by this)
       - nameless topics (stricter: characteristic-less topics)
       - topics with no unscoped name
       - duplicate occurs
       - duplicate names
       - schema stuff, when we get it       
       - XML-specific
         - topics that don't exist
         - topic naming constraint
         - identity duplication


Fint om du sender meg mail hver gang du mener at du har implementert
en av disse ferdig, så jeg kan se på det.

Java.

--Lars M.

